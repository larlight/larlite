###################################################

# default target list
ROOTMAP=$(SHLIB:.so=.rootmap)
LARLITE_BASIC_ROOTLIBS = $(shell root-config --libs)

all: pre_build main_build 

pre_build:

main_build: .depend $(DICT).cxx $(LARLITE_LIBDIR)/$(SHLIB) $(LARLITE_LIBDIR)/$(ROOTMAP) 

# default rule for making .o files from .cxx files
%.o: %.cxx
	@echo '<< compiling' $< '>>'
	@$(CXX) $(CXXFLAGS) $(INCFLAGS) -c $<

ifdef LARLITE_ROOT6

# root class dictionary
$(DICT).cxx: $(HEADERS) LinkDef.h
	@echo '<< generating CINT >>'
	@rootcling -f $(DICT).cxx -s $(LARLITE_LIBDIR)/$(SHLIB) -rmf $(LARLITE_LIBDIR)/$(ROOTMAP) -c $(CINTFLAGS) $(INCFLAGS) $(HEADERS) LinkDef.h

# rule for making shareable library
$(LARLITE_LIBDIR)/$(SHLIB): $(OBJECTS) $(DICT).cxx $(DICT).o
	@echo '<< generating $(SHLIB) >>'
	@$(SOMAKER) $(SOFLAGS) $(LDFLAGS) $(LARLITE_BASIC_ROOTLIBS) -o $(LARLITE_LIBDIR)/$(SHLIB) $(filter-out $(DICT).o, $(OBJECTS)) $(DICT).o

# if ROOTMAP defined, define rule for making it
$(LARLITE_LIBDIR)/$(ROOTMAP): $(LARLITE_LIBDIR)/$(SHLIB)

else

# root class dictionary
$(DICT).cxx: $(HEADERS) LinkDef.h
	@echo '<< generating CINT >>'
	@rootcint -f $(DICT).cxx -c $(CINTFLAGS) $(INCFLAGS) $(HEADERS) LinkDef.h

# rule for making shareable library
$(LARLITE_LIBDIR)/$(SHLIB): $(OBJECTS) $(DICT).cxx $(DICT).o
	@echo '<< generating $(SHLIB) >>'
	@mkdir -p lib
	@$(SOMAKER) $(SOFLAGS) $(LDFLAGS) $(LARLITE_BASIC_ROOTLIBS) -o lib/$(SHLIB) $(filter-out $(DICT).o, $(OBJECTS)) $(DICT).o 
	@echo '<< installing $(SHLIB) >>'
	@ln -sf $(PWD)/lib/$(SHLIB) $(LARLITE_LIBDIR)

# if ROOTMAP defined, define rule for making it
$(LARLITE_LIBDIR)/$(ROOTMAP): $(LARLITE_LIBDIR)/$(SHLIB)
	@echo "<< generating rootmap >>"
	@cd lib && ./gen_rootmap.sh
	@echo "<< installing rootmap >>"
	@ln -sf $(PWD)/lib/$(SHLIB:.so=.rootmap) $(LARLITE_LIBDIR)

endif
###################################################

# "make clean" target
clean: pre_clean
	@echo '<< cleaning all >>'
	@rm -f .depend
	@rm -f $(DICT).* *.o 
	@rm -f lib/*.o lib/lib*.so lib/lib*.rootmap lib/lib*.pcm
	@rm -f $(LARLITE_LIBDIR)/$(SHLIB) $(LARLITE_LIBDIR)/$(SHLIB:.so=.rootmap) $(LARLITE_LIBDIR)/$(SHLIB:.so=_rdict.pcm)

pre_clean: 

###################################################
libclean:
	@echo '<< cleaning lib directory >>'
	@rm -f $(LARLITE_LIBDIR)/$(SHLIB) 
	@rm -f lib/$(SHLIB) 

###################################################
clearCINT:
	@echo '<< removing CINT >>'
	@rm -f $(DICT).*


###################################################
# auto-generate dependencies using g++ -M
.depend: $(SOURCES) $(HEADERS)
	@#rm .depend
	@echo '<< checking dependencies >>'
	@$(CXX) -M $(CXXFLAGS) $(INCFLAGS) $(SOURCES) > .depend

-include .depend

#ALWAYS:
