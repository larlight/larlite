import os,sys

DATA_TOP='../../../../data/pre_merging/LArLight/'

DATA_DIR=sys.argv[1]

ORIG='%s/%s' % (DATA_TOP_DIR,DATA_DIR)
if not os.path.isdir(ORIG): sys.exit(1)

OUT_DIR='%s_merged' % DATA_DIR
os.system('mkdir %s' % OUT_DIR)

out_name_1='combined_cluster_%02d_1stStage.root'
out_name_2='combined_cluster_%02d_2ndStage.root'

for x in xrange(20):
    os.system('python run_prelim'

